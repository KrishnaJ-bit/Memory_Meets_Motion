.env
.env.*
!.env.example

node_modules/
dist/
build/
coverage/

*.log
*.tmp
.DS_Store

demo/autopilot-monitor/events/*.jsonl
!demo/autopilot-monitor/events/.gitkeep
# Track 1 (capture/memory) runtime fixture stores — regenerate with the fixture adapters,
# never committed. Stable handoff fixtures live in fixtures/ instead.
.laserdata-fixtures/
.falkordb-fixtures/
.fixture-build/

# RocketRide's local component catalog (large, vendored, regenerated by the RocketRide tooling)
.rocketride/

# Guild agent working copies: each is its own git repo with the Guild server as
# its remote (guild agent clone/save). Reviewable mirrors of their source live
# beside them as agents/<name>.agent.ts.
agents/relay-resume/
agents/relay-context-summarizer/
agents/relay-pr-risk-review/
